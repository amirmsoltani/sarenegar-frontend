import styles from "./PillReminder.module.scss";

export const PillReminder = () => {
  return (
    <section className={styles.container}>
      <img className={styles.cover} src="./addPill.png" />
    </section>
  );
};
