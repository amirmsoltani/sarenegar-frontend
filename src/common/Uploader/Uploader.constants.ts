export const OneMbUInBytes = 1_000_000;

// ! sizes are in bytes format

export const PDF_FORMATS = [".pdf"];
// ? 10mb
export const MAX_PDF_SIZE = 10 * OneMbUInBytes;

export const IMAGE_FORMATS = [".jpg", ".jpeg", ".png", ".gif", ".webp"];
// ? 5mb
export const MAX_IMAGE_SIZE = 5 * OneMbUInBytes;

export const VIDEO_FORMATS = [".mp4", ".webm"];
// ? 1000mb
export const MAX_VIDEO_SIZE = 1000 * OneMbUInBytes;

export const AUDIO_FORMATS = [".mp3", ".aac", ".ogg"];
// ? 200mb
export const MAX_AUDIO_SIZE = 200 * OneMbUInBytes;

export const ODD_FORMATS = [".zip", ".rar", ".txt", ".ppt", ".pptx", ".doc", ".docx", ".csv", ".xls", ".xlsx"];
// ? 600mb
export const MAX_ODD_SIZE = 600 * OneMbUInBytes;
